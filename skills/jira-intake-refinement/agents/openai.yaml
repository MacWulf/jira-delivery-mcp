interface:
  display_name: "Jira Intake"
  short_description: "Refine backlog and route test follow-up"
  default_prompt: "Use -intake-refinement to classify Jira work items, enforce backlog readiness, and mark jira-quality-control as the follow-up for code-changing work before execution."
