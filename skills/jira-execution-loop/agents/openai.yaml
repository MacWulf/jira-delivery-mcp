interface:
  display_name: "Jira Execution"
  short_description: "Run delivery with test-plan gates"
  default_prompt: "Use -execution-loop to pick and activate Jira work, require jira-quality-control before code-changing implementation, record progress, validate evidence, and close only when Jira truly allows it."
